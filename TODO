Make tree.py
    height method
    Tree constructor:
        (data_A,
            (
                (data_B, (child_B1, child_B2, ...)),
                (data_C, (child_C1, child_C2, ...)),
                ...
            )
        )
    Tree iterators: (Iterative methods only!)
        breadth-first traversal
        pre-order traversal
        in-order traversal
        post-order traversal

Update most docstrings
